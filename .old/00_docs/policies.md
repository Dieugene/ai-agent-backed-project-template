# Политики работы с проектом

## 🚀 Быстрая навигация для архитектора

**Если у вас вопрос "Куда писать...?"** — смотрите раздел **"Где фиксировать что"** (строка 22+):

| Вопрос | Ответ | Строка |
|--------|-------|--------|
| Где описать архитектуру проекта? | `00_docs/architecture.md` | 48+ |
| Где описать техническую спецификацию (форматы данных, API)? | `00_docs/project_spec.md` | 81+ |
| Где описать работу с внешними API (Gemini, Cloud Vision)? | `00_docs/api_integration.md` | 106+ |
| Где указать, что v2 строится поверх v1? | `00_docs/AI_README.md` → раздел "Референсные проекты" | 133+ |
| Где документировать переменные окружения? | `00_docs/AI_README.md` → раздел "Переменные окружения" | 166+ |
| Где создать файл с описанием задачи? | `02_tasks/001_task_name/iteration_001.md` | 204+ |
| Где описать форматы данных Google Sheets? | `00_docs/project_spec.md` | 260+ |

**⚠️ ВАЖНО:** После создания ЛЮБОГО нового документа в `00_docs/` → ОБЯЗАТЕЛЬНО добавить его в `AI_README.md`, раздел "Дополнительные документы проекта".

---

## Общие принципы

### Управление задачами и взаимодействие с пользователем

- **Управление задачами:** через раздел "Состояние задач" в `AI_README.md`. TODO-листы и task-list файлы НЕ создаются.
- **Перед записью в файлы:** архитектор сначала обсуждает изменения с пользователем, затем записывает.

### Корневой каталог и политика размещения
- Рабочий корень проекта: папка с названием проекта (например, `my_project/`)
- **Любые новые файлы и папки создаём ТОЛЬКО внутри корня проекта**
- Соседние каталоги используются исключительно как справочные источники; запись туда запрещена
- Если требуется новая сущность (папка, файл), сначала согласуем с архитектором, затем добавляем

### Правило "один источник правды"
- Каждый тип артефакта имеет одно место хранения
- Если артефакт не помещается ни в одну из папок, эскалируем вопрос архитектору
- Архитектор обновляет эту таблицу политик при расширении структуры

### Нумерация
- Используем префиксы `00_`, `01_`, `02_` и т.д. для отражения логического порядка
- Нумерация помогает избежать зависимости от алфавитного сортирования
- Внутри папок задач и модулей используем трехзначную нумерацию: `001_`, `002_`, `003_`

---

## Где фиксировать что: Руководство для архитектора

Этот раздел отвечает на вопрос **"Куда писать?"** для разных типов информации.

### ⚠️ КРИТИЧЕСКИ ВАЖНОЕ ПРАВИЛО

**AI_README.md - это ЕДИНАЯ ТОЧКА ВХОДА для всех агентов.**

**При создании ЛЮБОГО нового документа в `00_docs/`:**

1. ✅ Создать сам документ (architecture.md, project_spec.md, и т.д.)
2. ✅ **ОБЯЗАТЕЛЬНО** добавить его в AI_README.md, раздел "Дополнительные документы проекта"
3. ✅ Указать краткое описание назначения документа

**Агент НЕ ДОЛЖЕН гадать, какие файлы читать. Всё явно перечислено в AI_README.md.**

**Пример обновления AI_README.md:**
```markdown
### Дополнительные документы проекта (если существуют):
- **`00_docs/architecture.md`** — детальное описание архитектуры проекта
- **`00_docs/project_spec.md`** — техническая спецификация, форматы данных
- **`00_docs/api_integration.md`** — работа с внешними API
```

---

### Архитектурные решения и технические спецификации

#### Вопрос: "Где описать архитектуру проекта (UI+Worker, Split Architecture, и т.д.)?"
**Ответ:** `00_docs/architecture.md`

**Создать отдельный файл** с детальным описанием:
- Общая архитектура (диаграммы, компоненты)
- Технологический стек и обоснование выбора
- Взаимодействие компонентов
- Deployment архитектура
- Масштабирование и производительность

**В AI_README.md добавить только краткую ссылку:**
```markdown
## Архитектура проекта

Проект использует Split Architecture (UI Service + Worker Job).

**Детальное описание архитектуры:** см. `00_docs/architecture.md`
```

**⚠️ ОБЯЗАТЕЛЬНО также добавить в раздел "Дополнительные документы проекта":**
```markdown
### Дополнительные документы проекта (если существуют):
- **`00_docs/architecture.md`** — детальное описание архитектуры проекта
```

**AI_README.md - это ЕДИНАЯ ТОЧКА ВХОДА.** Агент не должен гадать, какие файлы читать.
Все дополнительные документы перечисляются в AI_README.md явно.

---

#### Вопрос: "Где описать техническую спецификацию (форматы данных, API, протоколы)?"
**Ответ:** `00_docs/project_spec.md`

**Создать отдельный файл** с:
- Требования к функциональности
- Форматы входных/выходных данных
- JSON схемы, структуры таблиц
- API endpoints и контракты
- Протоколы взаимодействия
- Ограничения и лимиты

**⚠️ ОБЯЗАТЕЛЬНО обновить AI_README.md в двух местах:**

1. Добавить в раздел "Дополнительные документы проекта":
```markdown
- **`00_docs/project_spec.md`** — техническая спецификация, форматы данных
```

2. Добавить краткую ссылку в основной текст (опционально):
```markdown
**Техническая спецификация:** см. `00_docs/project_spec.md`
```

---

#### Вопрос: "Где описать работу с внешними API (Gemini, Cloud Vision, и т.д.)?"
**Ответ:** `00_docs/api_integration.md` или `00_docs/gemini_sdk_notes.md`

**Создать тематический файл** с:
- Используемые API и endpoints
- Методы аутентификации
- Форматы запросов/ответов
- Rate limits и квоты
- Примеры кода
- Обработка ошибок

**⚠️ ОБЯЗАТЕЛЬНО обновить AI_README.md:**

1. Добавить в раздел "Дополнительные документы проекта":
```markdown
- **`00_docs/api_integration.md`** — работа с внешними API
```

2. Добавить в раздел "Технологический стек / Внешние API":
```markdown
### Внешние API/сервисы
- Gemini API — детали см. `00_docs/gemini_sdk_notes.md`
- Cloud Vision API — детали см. `00_docs/api_integration.md`
```

---

### Референсные проекты и совместимость с предыдущими версиями

#### Вопрос: "Где описать, что v2 строится поверх v1? Где ссылки на референсный проект?"
**Ответ:** В `00_docs/AI_README.md`, раздел "Референсные проекты"

**Обязательно добавить раздел:**
```markdown
## Референсный проект v1

**Расположение:** `../04_a_reports_ETL/` (или указать путь)

**Что переиспользуем из v1:**
- Split Architecture (UI Service + Worker Job)
- Gemini REST API (без SDK)
- Деплой скрипты для GCP (`deploy_gcp.ps1`)
- Структура Google Sheets (листы конфигурации)

**Ключевые изменения в v2:**
- [Список изменений]
- [Новые функции]

**Правило для разработчика:**
Перед реализацией аналогичного функционала:
1. Изучить реализацию в v1: `../04_a_reports_ETL/03_src/`
2. Переиспользовать работающий код где возможно
3. Адаптировать под требования v2
4. Документировать изменения

**Детальная архитектура:** см. `00_docs/architecture.md`
```

---

### Переменные окружения и конфигурация

#### Вопрос: "Где документировать новые переменные окружения (GOOGLE_SHEET_ID и т.д.)?"
**Ответ:** В `00_docs/AI_README.md`, раздел "Переменные окружения"

**Обязательно указать:**
- ВСЕ переменные окружения (старые из v1 + новые)
- Назначение каждой переменной
- Где получить значения (ссылки на документацию)
- Какие обязательны, какие опциональны
- Примеры значений (без реальных ключей)

**Пример:**
```markdown
## Переменные окружения (.env)

### API Keys (из v1)
```bash
GEMINI_API_KEY=your_key_here          # Обязателен. Получить: https://...
```

### Google Sheets (новое в v2)
```bash
GOOGLE_SHEET_GOLD_ID=...              # Обязателен. ID эталонного файла
GOOGLE_SHEET_DIFF_ID=...              # Обязателен. ID файла диффов
GOOGLE_SHEET_LOG_ID=...               # Обязателен. ID файла логов
GOOGLE_MAPPING_FILE_ID=...            # Опционально. ID файла маппингов
```

### Service Account (из v1)
```bash
GOOGLE_APPLICATION_CREDENTIALS=path   # Обязателен. Путь к JSON
```
```

**Также создать:** `.env.example` в корне проекта с шаблоном

---

### Создание задач и итераций

#### Вопрос: "Где создать итерацию для разработчика? Достаточно ли описания в AI_README?"
**Ответ:** **НЕТ, недостаточно.** Обязательно создать папку задачи в `02_tasks/`

**Алгоритм для архитектора:**

**Шаг 1. Создать структуру задачи:**
```bash
mkdir -p 02_tasks/001_task_name/artifacts
```

**Шаг 2. Создать файл постановки:**
```bash
# Скопировать шаблон
cp ../00_dev_template/02_tasks/001_template_task/iteration_001.md \
   02_tasks/001_task_name/iteration_001.md
```

**Шаг 3. Заполнить `iteration_001.md`:**
- **Цель:** четкое описание цели итерации
- **Контекст:** ссылки на `architecture.md`, `project_spec.md`, референс v1
- **Задачи:** детальный пошаговый список (10-30 пунктов)
- **Технические требования:** что использовать, как реализовать
- **Артефакты:** что будет создано
- **Критерии завершения (DoD):** конкретные критерии приемки

**Шаг 4. Обновить `AI_README.md`:**
В таблице "Состояние задач" добавить:
```markdown
| T-001 | Краткое описание задачи | в работе | см. 02_tasks/001_task_name/ |
```

**Шаг 5. Передать разработчику промпт:**
```
Ты выступаешь в роли разработчика (см. 01_agents/roles.md).

Перед началом прочитай:
- 01_agents/roles.md
- 00_docs/AI_README.md
- 00_docs/policies.md
- 00_docs/architecture.md          # если есть
- 00_docs/project_spec.md          # если есть
- 02_tasks/001_task_name/iteration_001.md

Задача: Выполнить задачу, описанную в iteration_001.md

После выполнения сообщи постановщику.
```

**⚠️ ВАЖНО:** Промпт НЕ содержит деталей задачи. Все детали в `iteration_001.md`.

---

### Форматы данных и структуры

#### Вопрос: "Где описать форматы листов Google Sheets (extraction_<ts>, diff_<ts>, logs_<ts>)?"
**Ответ:** В `00_docs/project_spec.md`, раздел "Форматы данных"

**Обязательно указать:**
- Название листов и шаблон именования
- Структура колонок (порядок, названия, типы)
- Правила заполнения
- Цветовая кодировка (если есть)
- Примеры

**Также в `00_docs/architecture.md`:**
- Как компоненты взаимодействуют с Sheets
- Диаграмма потоков данных

---

### Решения и договоренности

#### Вопрос: "Где фиксировать решения (дифф: жёсткое равенство, семантический триаж без порогов, и т.д.)?"
**Ответ:** Зависит от типа решения:

**1. Архитектурные решения** → `00_docs/architecture.md`
- Выбор подхода (семантический триаж через VLM)
- Обоснование решения

**2. Технические детали** → `00_docs/project_spec.md`
- Алгоритмы (жёсткое равенство для диффа)
- Форматы и протоколы

**3. Текущие договоренности проекта** → `00_docs/AI_README.md`, раздел "Текущие договорённости"
```markdown
## Текущие договорённости

1. Дифф-сравнение использует жёсткое равенство (без tolerance)
2. Семантический триаж head/tail через VLM, без жёстких порогов страниц
3. Многочастные PDF обрабатываются как единый документ
4. Поле 30 и внешние поля пока неактивны (пусто/серый)
5. Не трогаем эталонный файл; отдельный файл для диффов, отдельный для логов
```

**4. Временные решения / TODO** → В конкретной задаче `02_tasks/NNN/iteration_NNN.md`

---

### Примеры файлов для разных сценариев

#### Сценарий 1: Проект с нуля
**Обязательные файлы:**
- `00_docs/AI_README.md` ✅
- `00_docs/policies.md` ✅
- `01_agents/roles.md` ✅

**Опциональные (создать по мере необходимости):**
- `00_docs/project_spec.md` — если есть техническая спецификация
- `00_docs/architecture.md` — если архитектура сложная

---

#### Сценарий 2: v2 поверх существующего v1
**Обязательные файлы:**
- `00_docs/AI_README.md` ✅ — с разделом "Референсный проект v1"
- `00_docs/policies.md` ✅
- `01_agents/roles.md` ✅
- `00_docs/architecture.md` ✅ — описать v1, v2, совместимость
- `00_docs/project_spec.md` ✅ — если форматы данных изменились

**В AI_README.md обязательно:**
- Раздел "Референсный проект v1" с путями и описанием
- Раздел "Технологический стек" — что переиспользуем
- Раздел "Переменные окружения" — старые + новые

---

#### Сценарий 3: Интеграция с внешними API
**Создать:**
- `00_docs/api_integration.md` или `00_docs/<api_name>_notes.md`
- Описать endpoints, аутентификацию, примеры

**В AI_README.md:**
- Раздел "Технологический стек / Внешние API"
- Ссылка на детальную документацию

---

## Политики для каждой папки

### 00_docs/ — Документация проекта

**Назначение:** Центральное хранилище всей проектной документации, спецификаций, инструкций

**Что хранится:**
- `AI_README.md` — стартовая точка для любого агента, центральный документ проекта
- `policies.md` — данный файл с политиками работы
- `project_spec.md` — техническая спецификация проекта (опционально)
- Дополнительные технические документы (API docs, SDK notes, architecture diagrams)

**Ответственный:** Оба агента (архитектор создает/обновляет, разработчик поддерживает техническую часть)

**Правила:**
- Документация пишется на русском или английском языке (согласно выбору проекта)
- Каждый документ должен иметь четкое назначение
- НЕ складываем сюда данные, логи или исходный код

**Обязательная структура AI_README.md:**
1. Назначение проекта
2. Роли и ответственность (ссылка на `01_agents/roles.md`)
3. Обязательная ссылка на `policies.md` в разделе "Перед началом работы"
4. Таблица структуры каталогов (Путь | Назначение | Ответственный | Что кладём)
5. Таблица состояния задач (ID | Описание | Статус | Комментарий)
6. Конвенции проекта (окружение, зависимости, запуск)
7. Стартовые промпты для каждой роли
8. Описание окружения для ключевых итераций

---

### 00_initial_docs/ — Исходные требования (опционально)

**Назначение:** Хранение первоначальных требований, внешних спецификаций, референсных материалов

**Что хранится:**
- Исходные требования заказчика
- Инструкции по извлечению данных (extraction instructions)
- Схемы данных (JSON schemas, DB schemas)
- Примеры входных/выходных данных
- Референсная документация внешних систем

**Ответственный:** Архитектор

**Правила:**
- Файлы в этой папке НЕ изменяются в процессе разработки
- Это "только для чтения" источник первоначальных требований
- Актуальная спецификация ведется в `00_docs/project_spec.md`

---

### 01_agents/ — Роли агентов

**Назначение:** Описание ролей, ответственности и правил взаимодействия агентов

**Что хранится:**
- `roles.md` — описание всех ролей и зон ответственности
- История диалогов архитектора (опционально, для анализа)
- Дополнительные правила взаимодействия

**Ответственный:** Архитектор

**Правила:**
- Каждая роль должна иметь четкое описание зоны ответственности
- Обязательно указывать ограничения (что НЕ делает роль)
- Описывать workflow взаимодействия между ролями
- НЕ хранить здесь код или данные

---

### 02_tasks/ — Итерации и задачи разработки

**Назначение:** Хранение постановок задач, итераций разработки и отчетов о выполнении

**Структура:**
```
02_tasks/
├── 001_task_name/
│   ├── iteration_001.md        # Постановка + Отчет разработчика
│   └── artifacts/               # Специфические артефакты задачи (опционально)
├── 002_another_task/
│   ├── iteration_002.md
│   └── artifacts/
└── README.md
```

**Что хранится:**
- Каждая задача в отдельной папке с трехзначной нумерацией (`001_`, `002_`, ...)
- Файл `iteration_NNN.md` содержит:
  - Цель итерации/задачи
  - Детальные задачи (пошаговый список действий)
  - Технические требования
  - Артефакты (что будет создано)
  - Критерии завершения (Definition of Done)
  - Раздел "Отчёт разработчика" (добавляется в конце после выполнения)
- Папка `artifacts/` для специфических артефактов задачи (скриншоты, промежуточные данные)

**Ответственный:**
- Архитектор создает папку и файл постановки
- Разработчик добавляет раздел "Отчёт разработчика"

**Правила:**
- Одна задача = одна папка
- Нумерация строго последовательная (001, 002, 003, ...)
- Название папки кратко отражает суть задачи (латиница, snake_case)
- Постановка задачи содержит ВСЕ детали; промпт агенту НЕ дублирует детали
- Разработчик ОБЯЗАН добавить раздел "Отчёт разработчика" в конец файла
- Глобальные артефакты (данные, логи) хранятся в `04_data/` и `05_logs/`, а не в `artifacts/`

**Формат iteration_NNN.md:**
```markdown
# Iteration NNN — Краткое название

## Цель
Описание цели итерации

## Задачи
1. Первая задача
2. Вторая задача
...

## Технические требования
- Требование 1
- Требование 2

## Артефакты
- Что будет создано/изменено
- Пути к файлам

## Критерии завершения (DoD)
1. Критерий 1
2. Критерий 2

---

## Отчёт разработчика
*(Заполняется после выполнения)*

### Выполненные работы
- Описание

### Созданные/измененные файлы
- Список файлов с путями

### Артефакты
- Пути к данным, логам, результатам

### Результаты тестов/проверок
- Описание проверок

### Вопросы/риски
- Список вопросов или рисков
```

---

### 03_src/ — Исходный код

**Назначение:** Рабочий код проекта — CLI, библиотеки, скрипты, конфигурации

**Структура:**
```
03_src/
├── 001_cli/                    # CLI интерфейс
├── 002_core_module/            # Основной модуль
├── 003_data_processing/        # Обработка данных
└── README.md
```

**Что хранится:**
- Каждый модуль/компонент в отдельной папке с трехзначной нумерацией
- Исходный код на Python/JavaScript/и т.д.
- Конфигурационные файлы модулей
- Dockerfile, docker-compose.yml
- requirements.txt, package.json и другие файлы зависимостей

**Ответственный:** Разработчик

**Правила:**
- НЕ складываем сюда входные/выходные данные — только исходный код и конфигурации
- Каждый модуль должен иметь свой `README.md` с описанием назначения
- Нумерация отражает логический порядок загрузки/запуска модулей
- Зависимости фиксируются в `requirements.txt` (Python) или аналогичных файлах
- Виртуальное окружение `.venv` создается в корне проекта (не в `03_src/`)

**Примеры модулей:**
- `001_cli/` — точка входа, CLI команды
- `002_core/` — основная бизнес-логика
- `003_data_processing/` — обработка данных
- `004_api_client/` — клиенты внешних API
- `005_utils/` — вспомогательные утилиты

---

### 04_data/ — Рабочие данные

**Назначение:** Хранение всех данных, генерируемых в процессе работы проекта

**Что хранится:**
- Входные данные (если они не слишком большие)
- Промежуточные результаты обработки
- Выходные данные (JSON, CSV, изображения)
- Индексы, кэши
- Специализированные подпапки для разных типов данных

**Ответственный:** Разработчик (автоматическая генерация пайплайном)

**Правила:**
- Структура внутри `04_data/` зависит от специфики проекта
- Рекомендуется создавать подпапки по типам данных или по источникам
- НЕ хранить здесь JSON с логами или метаданными запусков (это в `05_logs/`)
- НЕ хранить здесь код или документацию

**Примеры подпапок:**
- `04_data/pages/` — изображения страниц PDF
- `04_data/tables/` — извлеченные таблицы
- `04_data/processed/` — обработанные данные
- `04_data/cache/` — кэшированные данные

---

### 05_logs/ — Логи запусков

**Назначение:** Лог-файлы, диагностические отчеты, метаданные о запусках

**Структура:**
```
05_logs/
├── run_20251216_143000/        # Каждый запуск в отдельной папке
│   ├── run.log                  # Основной лог
│   ├── metadata.json            # Метаданные запуска
│   └── usage.json               # Usage статистика (API calls, tokens)
└── README.md
```

**Что хранится:**
- Каждый запуск пайплайна создает подпапку `run_YYYYMMDD_HHMMSS/`
- Логи выполнения (текстовые файлы `.log`)
- Метаданные запусков (JSON с параметрами, конфигурацией)
- Usage статистика (количество вызовов API, токены, время выполнения)
- Диагностические отчеты, дампы ошибок

**Ответственный:** Разработчик

**Правила:**
- НЕ смешиваем логи разработки и рабочие логи
- Для ручных заметок используем `00_docs/`
- При анализе проблем прикладываем ссылки на конкретный лог в постановке задачи
- Формат timestamp: `YYYYMMDD_HHMMSS`
- В логах фиксируем версии компонентов, параметры запуска, пути к артефактам

---

## Правила формирования промптов для агентов

### Структура промпта для разработчика

**ВАЖНО:** Промпт НЕ дублирует конкретные действия из постановки задачи!

**Правильная структура:**
```
Ты выступаешь в роли разработчика (см. 01_agents/roles.md).

Перед началом прочитай:
- 01_agents/roles.md                    # для понимания своей роли
- 00_docs/AI_README.md                  # для понимания контекста проекта
- 00_docs/policies.md                   # для понимания политик работы
- 02_tasks/NNN_task_name/iteration_NNN.md    # ПОСТАНОВКА ЗАДАЧИ

Задача: Выполнить задачу, описанную в iteration_NNN.md

После выполнения сообщи постановщику.
```

**Принципы:**
- Промпт только направляет к файлам с контекстом и постановкой
- Все конкретные действия описаны в файле `iteration_NNN.md`
- Промпт остается коротким и универсальным
- Детальное описание всегда лежит в перечисленных файлах

### Структура промпта для архитектора

```
Ты выступаешь в роли архитектора (см. 01_agents/roles.md).

Перед началом прочитай:
- 01_agents/roles.md                    # обязанности и формат взаимодействия
- 00_docs/AI_README.md                  # текущее состояние проекта
- 00_docs/policies.md                   # политики работы
- 02_tasks/NNN_task_name/iteration_NNN.md    # контекст предыдущих итераций

Задача: [краткое описание задачи архитектора, например "Сформировать следующую итерацию"]

После выполнения передай разработчику промпт для исполнения.
```

---

## Конвенции окружения

### Python проекты
- Виртуальное окружение создается в корне проекта: `python -m venv .venv`
- Зависимости фиксируются в `requirements.txt` в корне проекта
- Активация окружения:
  - Windows: `.venv\Scripts\activate`
  - Linux/Mac: `source .venv/bin/activate`
- Секреты и ключи API хранятся в `.env` файле (в корне проекта)
- `.env` файл НЕ коммитится в Git (добавить в `.gitignore`)

### Node.js проекты
- Зависимости в `package.json` в корне проекта
- `node_modules/` НЕ коммитится в Git
- Секреты в `.env` файле или используется `dotenv` пакет

### Docker проекты
- `Dockerfile` и `docker-compose.yml` размещаются в корне проекта или в `03_src/`
- Контейнеры должны быть воспроизводимыми
- Документировать переменные окружения

---

## Общий workflow работы над задачей

1. **Формулировка** (Архитектор):
   - Обсуждает задачу с пользователем (детали, инструкции, как выполнять прогон)
   - Изучает предыдущие задачи и их реализацию
   - Обновляет требования в `00_docs/` при необходимости
   - Создает папку `02_tasks/NNN_task_name/`
   - Пишет файл `iteration_NNN_dev.md` с полной постановкой

2. **Регистрация** (Архитектор):
   - Заносит задачу в раздел "Состояние задач" в `AI_README.md`
   - Устанавливает статус "в работе"

3. **Передача разработчику** (Архитектор):
   - Формирует короткий промпт (см. раздел "Правила формирования промптов")
   - Вставляет промпт в чат

4. **Исполнение** (Разработчик):
   - Читает все указанные в промпте файлы
   - Реализует задачу согласно постановке
   - Обновляет соответствующие файлы/папки в `03_src/`, `04_data/`, `05_logs/`

5. **Отчет** (Разработчик):
   - Подготавливает отчет: описание работ, пути к артефактам, результаты тестов
   - Уведомляет архитектора о завершении

6. **Ревизия** (Архитектор):
   - Выслушивает отчет разработчика
   - **Самостоятельно проверяет** артефакты (код в `03_src/`, данные в `04_data/`, логи в `05_logs/`)
   - Проверяет соответствие критериям завершения (DoD)
   - **НЕ полагается только на слова в отчете** — проверяет факты
   - **Вносит отчет** разработчика в файл `iteration_NNN_dev.md` (ТОЛЬКО после успешной проверки)
   - Обновляет статус задачи в `AI_README.md` на "выполнена"
   - При необходимости добавляет новые требования для следующих итераций

---

## Контроль версий (Git)

### Что коммитить:
- Весь код в `03_src/`
- Всю документацию в `00_docs/`, `00_initial_docs/`, `01_agents/`
- Все постановки задач в `02_tasks/`
- README файлы
- Конфигурационные файлы (requirements.txt, package.json, Dockerfile)

### Что НЕ коммитить:
- `.venv/`, `node_modules/`, `__pycache__/`
- `.env` файлы с секретами
- Большие файлы данных в `04_data/` (рассмотреть Git LFS или отдельное хранилище)
- Логи в `05_logs/` (кроме примеров для документации)
- Временные файлы, кэши

### .gitignore рекомендации:
```
# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Node
node_modules/

# Environment
.env
.env.local

# Data and logs
04_data/
05_logs/

# IDE
.vscode/
.idea/
*.swp
```

---

## Безопасность

### Секреты и ключи API:
- НИКОГДА не хардкодить секреты в коде
- Использовать `.env` файлы для локальной разработки
- Для production использовать секреты менеджеры (AWS Secrets Manager, Google Secret Manager, и т.д.)
- Документировать необходимые переменные окружения в `AI_README.md`

### Примеры переменных в .env:
```
# API Keys
GEMINI_API_KEY=your_key_here
OPENAI_API_KEY=your_key_here

# Optional endpoints
GEMINI_API_ENDPOINT=https://custom-proxy/v1beta/...
```
